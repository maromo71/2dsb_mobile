let base = 5
let altura = 8

function calcularAreaTriangulo(base, altura){
    return 0.5 * base * altura
}

let area = calcularAreaTriangulo(base, altura)
console.log(`A área possui area de ${area}`)

